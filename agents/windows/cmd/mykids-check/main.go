// Команда mykids-check — окно проверки MyKids на этой машине.
//
// Отдельная программа, а не режим агента: её запускают двойным щелчком, без
// командной строки и без установки чего бы то ни было. Всё, что она делает, —
// спрашивает у Windows то же, что спрашивает агент, и говорит, работает это
// или нет.
//
// Нужна потому, что весь Win32-слой — активное окно, простой, блокировка
// экрана, оверлей, запуск в чужой сессии — на настоящей Windows проверить
// иначе нечем: тесты идут на Linux, а просить родителя разбирать вывод
// консольной команды значит просить его же и решать, хорошо это или плохо.
package main

import "fmt"

// version подменяется при сборке: см. build.sh.
var version = "0.5.0"

func main() {
	if err := run(); err != nil {
		fail(fmt.Sprintf("Не удалось открыть окно проверки: %v", err))
	}
}
